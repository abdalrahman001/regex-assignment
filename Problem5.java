private static void processProblem5(BufferedReader reader, BufferedWriter writer) throws IOException {
        String variablePattern = "^[a-zA-Z_][a-zA-Z0-9_]*$";
        processProblem(reader, writer, variablePattern);
    }