
private static void processProblem6(BufferedReader reader, BufferedWriter writer) throws IOException {
        String oddBPattern = "^(.*[bB].*[bB].*)*[bB].*$";
        processProblem(reader, writer, oddBPattern);
    }